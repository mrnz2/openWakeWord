# Zależności treningu (openWakeWord z klonu Git — NIE instaluj pakietu openwakeword z PyPI:
# PyPI ciągnie speexdsp_ns / tflite-runtime z kołami tylko dla wybranych wersji Pythona).
#
# torch + torchaudio instaluje colab/install_colab_deps.py (osobno, z indeksem CUDA lub PyPI).

# TF 2.18 (tensorflow-cpu<2.19): numpy<2.1,>=1.26 — na Colab lepiej 2.0.x (mniej konfliktów z jax/opencv niż 1.26).
numpy>=2.0.0,<2.1.0
scipy>=1.3,<2
tqdm>=4,<5
requests>=2,<3
pyyaml>=6,<7
scikit-learn>=1,<2
# TF 2.18: protobuf<6,>=3.20; Colab (grpcio-status, ydf, …) wymaga ~5.29+ — unikamy downgrade do 4.x.
protobuf>=5.29.1,<6

onnx>=1.14,<1.17
onnxruntime>=1.16,<2

torchinfo>=1.8,<2
# >=1.4: bez pkg_resources w imports (Python 3.12 + Debian /usr/lib — ImpImporter). openWakeWord v0.6.0: Recall(..., task='binary') OK.
torchmetrics>=1.4.0,<2
speechbrain>=0.5.14,<1
mutagen>=1.46,<2
audiomentations>=0.30,<1
torch-audiomentations>=0.11,<1
acoustics>=0.2.6,<1
pronouncing>=0.2,<1
julius
webrtcvad
espeak-phonemizer
# piper-sample-generator używa espeak_phonemizer, nie piper_phonemize — pakiet rhasspy/piper-phonemize nie jest wymagany.
