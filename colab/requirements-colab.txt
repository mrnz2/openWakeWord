# Zastąpione przez instalację wieloetapową (uniknięcie konfliktów PyPI + Python 3.12).
#
# W Colab uruchom z katalogu projektu:
#   python colab/install_colab_deps.py
#
# Pliki:
#   requirements-colab-train.txt  — trening (torch instaluje skrypt osobno)
#   requirements-colab-tflite.txt — tensorflow + onnx2tf po treningu
